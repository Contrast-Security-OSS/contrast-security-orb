description: >
  Download and install the Contrast security agent

usage:
  version: 2.1

  orbs:
    contrast: contrast/contrast-agent@x.y.z

  jobs:
    setup:
      executor: contrast/default
      steps:
        - checkout

        - contrast/download

        - contrast/install

  workflows:
    setup:
      jobs:
        - setup
